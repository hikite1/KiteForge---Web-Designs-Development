name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm test

  # Deploys are handled by Netlify's GitHub integration:
  # - every PR gets a deploy preview
  # - pushes to main deploy to production
  # Build config lives in netlify.toml, not here.
